# Rsync-Move

A Bash wrapper around rsync to move/copy files/directories using rsync

```
Copy or Move files using Rsync.
Usage: rsync_move.sh [option] [Sources] [Destination]

  -c copy   files
  -m move   files
  --check   check files using rsync checksum after copying
```
